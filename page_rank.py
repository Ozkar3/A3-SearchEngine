import numpy as np
from check_link import check_link_exists

def pagerank(num_iterations: int = 100):
    damping_factor = 0.85
    N = 100  # Number of pages, just set to 100 for now
    
    v = np.ones(N) / N

    pass
